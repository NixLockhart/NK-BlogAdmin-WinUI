<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Blog_Manager.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Blog_Manager.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar"
                  Grid.Row="0"
                  Title="Blog Manager"
                  MinHeight="48"
                  IsPaneToggleButtonVisible="False"
                  IsBackButtonVisible="False">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" MaxWidth="450" Padding="40"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">

            <TextBlock Text="博客管理系统"
                      FontSize="28"
                      FontWeight="SemiBold"
                      Margin="0,0,0,24"
                      HorizontalAlignment="Center"/>

            <!-- 后端服务器选择 -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="BackendComboBox"
                         Grid.Column="0"
                         Header="后端服务器"
                         PlaceholderText="选择或添加后端服务器"
                         ItemsSource="{x:Bind BackendServers, Mode=OneWay}"
                         SelectedItem="{x:Bind SelectedBackend, Mode=TwoWay}"
                         DisplayMemberPath="DisplayName"
                         HorizontalAlignment="Stretch"
                         SelectionChanged="BackendComboBox_SelectionChanged"/>

                <Button x:Name="TestBackendButton"
                       Grid.Column="1"
                       Content="测试"
                       VerticalAlignment="Bottom"
                       Margin="8,0,0,0"
                       Click="TestBackendButton_Click"
                       IsEnabled="{x:Bind SelectedBackend, Mode=OneWay, Converter={StaticResource ObjectToBoolConverter}}"/>
            </Grid>

            <!-- 添加自定义后端按钮 -->
            <Button x:Name="AddCustomBackendButton"
                   Content="添加自定义后端"
                   HorizontalAlignment="Stretch"
                   Margin="0,0,0,16"
                   Click="AddCustomBackendButton_Click"/>

            <TextBox x:Name="UsernameTextBox"
                    Header="用户名"
                    PlaceholderText="请输入用户名"
                    Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,16"/>

            <PasswordBox x:Name="PasswordBox"
                        Header="密码"
                        PlaceholderText="请输入密码"
                        Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,0,8"
                        KeyDown="PasswordBox_KeyDown"/>

            <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                      Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                      Visibility="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"
                      Margin="0,0,0,16"
                      TextWrapping="Wrap"/>

            <Button Content="登录"
                   Command="{x:Bind ViewModel.LoginCommand}"
                   Style="{StaticResource AccentButtonStyle}"
                   HorizontalAlignment="Stretch"
                   IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>

            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                         Margin="0,16,0,0"
                         HorizontalAlignment="Center"/>
        </StackPanel>
        </Grid>

        <!-- Toast 通知容器 -->
        <Grid Grid.Row="1"
              HorizontalAlignment="Center"
              VerticalAlignment="Top"
              Margin="0,12,0,0"
              Canvas.ZIndex="100"
              IsHitTestVisible="False">
            <StackPanel x:Name="NotificationContainer"
                        Spacing="8"
                        IsHitTestVisible="True"/>
        </Grid>
    </Grid>
</Page>
